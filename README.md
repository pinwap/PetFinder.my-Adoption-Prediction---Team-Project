# PetFinder.my Adoption Prediction – Team Project

## 🎯 เป้าหมายโปรเจกต์
สร้างระบบ AI เพื่อทำนายความเร็วในการถูกรับเลี้ยงสัตว์ (ระดับ 0–4)
โดยใช้ข้อมูลหลายรูปแบบ:
- ข้อมูลตาราง (CSV)
- ข้อความอธิบาย
- รูปภาพ

โปรเจกต์นี้เป็น **Hybrid System**
ที่ใช้ทั้ง Classical Machine Learning และ Deep Learning

## 🧠 แนวคิดหลักของระบบ
- ใช้ Deep Learning เพื่อแปลงข้อมูลที่ซับซ้อน (รูป, ข้อความ) → ตัวเลข
- ใช้ Tree-based Model (LightGBM / CatBoost) เพื่อทำนายผลลัพธ์สุดท้าย
- แยกขั้นตอน “สร้าง feature” ออกจาก “เทรนโมเดล” เพื่อให้ทำงานเป็นทีมได้ง่าย

## 🌳 โครงสร้าง github branch
```code
main
 ├── dev
 │    ├── feature/tabular-features
 │    ├── feature/text-features
 │    ├── feature/image-features
 │    ├── model/lgbm-regression
 │    └── fix/missing-values
```
>  ทำงานแตก branch จาก dev -> เสร็จแล้ว PR เข้า dev -> เทสผ่าน ค่อย merge เข้า main

## 📁 โครงสร้างโปรเจกต์
```code
data/        ข้อมูลดิบจาก Kaggle
features/    ข้อมูลที่แปลงเป็นตัวเลขแล้ว (พร้อมใช้)
src/
  ├── make_features/   โค้ดสร้าง feature
  ├── train/           โค้ดเทรนโมเดล
  └── ensemble.py
submissions/ ไฟล์ส่ง Kaggle
```

## 👥 วิธีทำงานเป็นทีม
### 🟦 ฝ่าย Data 
- แปลงข้อมูลดิบเป็นตัวเลข = feature
- ไม่ต้อง train model
- ส่งมอบไฟล์ .parquet ใน features/
  - ทุกไฟล์ต้องมี PetID
  - ไม่มี missing
  - เป็นตัวเลขล้วน
### 🟩 ฝ่าย Model
- โหลด feature file มา train โมเดล
- ประเมินด้วย QWK
- ไม่ preprocess ข้อมูลซ้ำ
- ไม่ยุ่งโค้ด make_features
### 🟨 ฝ่าย Ensemble
- รวม feature ทุกแบบ
- รวมผลหลายโมเดล
- optimize threshold
- ทำ submission
